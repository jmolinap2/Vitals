<Window x:Class="Vitals.Settings.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ajustes de Vitals" Height="440" Width="360"
        WindowStartupLocation="CenterScreen"
        Background="#12141A"
        ResizeMode="NoResize">
    <Window.Resources>
        <SolidColorBrush x:Key="TextBrush" Color="#E7EEF0"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#8CA0A7"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#4D8CFF"/>
        <SolidColorBrush x:Key="RowBrush" Color="#1B2028"/>
        <SolidColorBrush x:Key="BorderBrush2" Color="#2C333C"/>

        <Style TargetType="Button" x:Key="IconButton">
            <Setter Property="Background" Value="{StaticResource RowBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Métricas" FontSize="18" FontWeight="SemiBold"
                   Foreground="{StaticResource TextBrush}" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="Activa las que quieras ver y ordénalas con las flechas."
                   FontSize="12" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,14"
                   TextWrapping="Wrap"/>

        <Grid Grid.Row="2" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Tamaño de la píldora" VerticalAlignment="Center"
                       Foreground="{StaticResource TextBrush}" FontSize="14"/>
            <ComboBox x:Name="SizeCombo" Grid.Column="1" Width="140" SelectedIndex="1">
                <ComboBoxItem Content="Pequeño"/>
                <ComboBoxItem Content="Normal"/>
                <ComboBoxItem Content="Grande"/>
                <ComboBoxItem Content="Muy grande"/>
            </ComboBox>
        </Grid>

        <ListBox x:Name="MetricsList" Grid.Row="3" Background="Transparent" BorderThickness="0">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource RowBrush}" BorderBrush="{StaticResource BorderBrush2}"
                            BorderThickness="1" CornerRadius="6" Margin="0,4" Padding="10,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" IsChecked="{Binding Enabled}"
                                      VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Grid.Column="1" Text="{Binding DisplayName}" VerticalAlignment="Center"
                                       Foreground="{StaticResource TextBrush}" FontSize="14"/>
                            <Button Grid.Column="2" Content="&#9650;" Width="26" Height="26" FontSize="9"
                                    Style="{StaticResource IconButton}" Click="MoveUp_Click" Margin="0,0,4,0"/>
                            <Button Grid.Column="3" Content="&#9660;" Width="26" Height="26" FontSize="9"
                                    Style="{StaticResource IconButton}" Click="MoveDown_Click"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Guardar y reiniciar Vitals" Padding="14,9" Click="Save_Click"
                    Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0"
                    Style="{StaticResource IconButton}"/>
        </StackPanel>
    </Grid>
</Window>
